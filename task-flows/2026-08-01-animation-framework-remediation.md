# Task Flow - Animation framework remediation (2026-08-01)

## Objective

- Preserve the current rendered appearance and code in a recoverable baseline commit before changing animation ownership.
- Remove every active site-owned animation path that bypasses CineView's `Animate`, `AnimateVideo`, `infiniteAnimation`, or phase-gated canvas contract.
- Preserve layout, typography, framing, timing intent, and scene composition while changing ownership.
- Verify drag and scroll behavior in real Chromium on desktop and mobile, with implementation and acceptance performed by separate agents.

## Baseline Evidence

- [x] B1 - Record the current worktree and exclude one-off probe artifacts from the baseline commit.
- [x] B2 - Capture current `/` and `/drag` desktop/mobile screenshots, including all five drag acts.
- [x] B3 - Commit the current product source, assets, tests, task records, acceptance script, and visual baseline.

## Remediation Nodes

- [x] R1 - Replace Act 1 ambient Framer Motion/CSS loops/ungated canvas and drag-direction CSS transitions with framework-owned lanes or phase-gated canvas output.
- [x] R2 - Replace Act 3 site-owned video seeking/transport animation with `AnimateVideo` while preserving V1/media timing, subtitles, fullscreen crop, and blur/fade transitions.
- [x] R3 - Move Act 4 timecode opacity and eclipse limb opacity/scale into framework-owned animation lanes; retain only unsupported text/SVG rendering as MotionValue consumers.
  - [x] R3a - Restore the `DESIGN.md` keyframe contract in the shared drag/scroll interpolation path so ordered hold-then-fade timing is framework-owned.
  - [x] R3b - Move the timecode's 65% reset / 35% fade and the eclipse limb's late opacity/scale onset onto `Animate` lanes.
  - [x] R3c - Re-read Act 4 ownership and hot paths, run focused checks, and compare desktop/mobile Act 4 against the committed baseline.
- [x] R4 - Replace home background, Hero, Capability, Demo subtitle, and DragPhone framework-bypass animations without changing layout or visual hierarchy.
  - [x] R4a - Remove the root scroll-state background driver and migrate Hero beam/hint loops to phase-gated `infiniteAnimation`.
  - [x] R4b - Remove Capability per-frame React state, render-prop supported-property writes, and CSS infinite loops while preserving hover and authored sequencing. (Reopened correction completed: film selection transitions now belong to framework lanes.)
  - [x] R4c - Replace Demo subtitle's per-frame supported-property DOM writers with framework lanes; retain only unsupported text/color projection if still required.
  - [x] R4d - Remove DragPhone's direct Framer Motion progress layer and REC CSS loop in favor of nested CineView timelines and `infiniteAnimation`.
  - [x] R4e - Re-read homepage ownership/hot paths, run focused checks, and compare all five desktop/mobile scenes against baseline.
- [x] R5 - Remove dead animation writers/hooks and scan the full site for remaining direct motion imports, CSS keyframes, ungated rAF, per-frame React state, and manual supported-property writes.

## Verification Nodes

- [x] V1 - After each remediation node, re-read changed owners for unique writers, dead code, file growth, and per-frame cost.
- [ ] V2 - Run formatting, site/framework type checks, focused contracts, full tests, lint, and production build.
- [x] V3 - Compare post-fix desktop/mobile screenshots against the committed visual baseline for layout displacement, crop changes, missing effects, and timing regressions.
- [x] V4 - Independent agent validates complete `/drag` and `/` scroll paths in real Chromium, including reverse/re-entry and concurrent animation smoothness.
- [x] V5 - Resolve all findings, repeat static and browser verification, and record authoritative evidence below.
  - [x] V5a - Reset outgoing Act 3 media ownership and transport projection at the terminal `idle` handoff, then independently recheck reverse re-entry.
  - [x] V5b - Replace the site's Chinese document metadata with English defaults.
  - [x] V5c - Reset Act 3's read-only transport projection on rushed reverse re-entry without adding a second video seek owner.
  - [x] V5d - Profile Act 2's independently reported frame pacing, isolate canvas versus DOM-loop cost, and remove avoidable continuous canvas work while preserving the phase-gated effect.

## Evidence Log

- Baseline capture: `scripts/capture-animation-snapshots.mjs --label baseline --port 3000` produced 20 viewport screenshots and `report.json` under `output/playwright/animation-baseline`.
- Baseline browser evidence: all five `/drag` scenes settled at `top=0` on 1440x900 and 390x844; all five `/` scroll scenes were advanced with real wheel input to their center-lock segment ends.
- Baseline runtime evidence: 0 console warnings/errors and 0 page errors across both routes and viewports.
- Baseline visual review: `contact-drag.png` and `contact-home.png` show the expected five distinct scenes on desktop and mobile; no blank or misidentified capture remains.
- Baseline commit completed after site type-check, diff check, pre-commit ESLint/Prettier, and 32 focused contract tests passed.
- R1 implementation: moved the ambient rig into Act 1, replaced its direct Framer Motion sink and four CSS loops with scene-owned `Animate`/`infiniteAnimation` lanes, phase-gated the dust canvas, moved animated grain into per-scene infinite lanes, removed drag-direction CSS transitions, and moved the hourly rollover onto drag arrival lanes. The root callback/tween implementation and unused drag progress owner were deleted (net -306 lines across the four production files).
- R1 self-review: Scene render/element/release ownership is unchanged; no React state was added to a per-frame path; only the canvas rAF remains and now cancels on `idle`/`exited`; direct active Framer Motion imports, CSS keyframes, root drag direction state, and the old sink owner scan clean in the changed scope.
- R1 verification: `git diff --check`, site type-check, focused ESLint, and the 20-capture `animation-r1` browser run passed with 0 console messages and 0 page errors. All recorded `/drag` geometry and page metrics match the baseline exactly on 1440x900 and 390x844; visual review confirms the Act 1 light, texture, dial, title and actions retain their baseline composition (wall-clock hour is expected to differ).
- R2 implementation: replaced Act3Media's native `currentTime` writer with a scene-owned `AnimateVideo` lane (`delay=3200ms`, `enter=3300ms`). Act3Media now only projects the shared scene clock to selection datasets, V2 subtitle selection, playhead position and timecode text; the existing preview Animate still owns blur/fade entrance and exit.
- R2 self-review: site source has zero Act 3 `currentTime` reads/writes; `VideoFrameRenderer` is the sole seek owner. The remaining transport effect writes only unsupported projection outputs and uses MotionValue subscriptions without React state. The source asset is exactly 6.000s, so the default full-range AnimateVideo mapping preserves the authored range without native autoplay handoff.
- R2 verification: site type-check, focused ESLint and six contracts (67 tests) passed. The 20-capture `animation-r2` run completed with 0 console/page errors, all `/drag` geometry exactly matches baseline, and desktop/mobile Act 3 visual comparison preserves fullscreen crop, timeline framing and final V2 state. A 34-sample real-browser transport probe found 21 active samples, monotonic video time, `0.000s` maximum delta between AnimateVideo and the authored transport projection, and a shared 6.000s endpoint.
- R3 implementation: restored full keyframe-sequence scrub interpolation (including shared/property `transition.times`) in the common drag/scroll property path, with a WeakMap cache keyed by stable variant/property/from-value so arrays and timing tables compile once rather than allocating per frame. Scalar variants retain the existing direct lerp path. Act 4's existing timecode lane now owns the 65% hold / 35% fade keyframes, and a dedicated late-onset Animate lane owns eclipse-limb opacity/scale. The remaining MotionValue effects write only unsupported timecode text and SVG `strokeDashoffset` projections.
- R3 self-review: timecode and limb source files contain zero manual `style.opacity` / `style.transform` writers; the shared scene/render/release owners are unchanged; no React state or new progress writer was introduced. Keyframed properties pay a cached Map lookup plus segment lerp per frame, with zero per-frame arrays; scalar properties add only the array-type guard required to preserve the public keyframe contract. The unrelated Act 3 contract import left unused by R2 was removed so framework type-check is clean.
- R3 verification: framework + site type-check, focused ESLint, diff check and 36 focused keyframe/drag/site contract tests passed. The first browser pass was explicitly rejected because the site consumes built `dist`; after rebuilding the framework and restarting Vite with forced dependency optimization, the dynamic Act 4 probe showed `00:00:00:03 / opacity 1` immediately before `00:00:00:00 / opacity 0.982`, followed by continuous `0.857 / 0.732 / 0.607 / 0.482` fade samples. The authoritative `animation-r3` run produced 20/20 captures, 0 console/page errors, and zero `/drag` desktop/mobile geometry mismatches against baseline. Final Act 4 desktop/mobile composition matches baseline; homepage-only scroll-hint sampling drift remains within 0.50px and belongs to R4's active loop remediation.
- R4a implementation: deleted the root `BackgroundRibbon`/`useScrollProgress`/LUT writer path, assigned each homepage Scene its settled static palette, and moved the Hero slogan beam and scroll-hint loops onto phase-gated `infiniteAnimation` lanes. The old render-prop state handshake and both Hero CSS keyframe loops were removed.
- R4a self-review: the homepage no longer performs a React state update for every root scroll frame; Hero has no local state, direct motion import, CSS animation, or rAF. The shared beam clock writes only inherited custom properties from the framework-owned infinite lane, and the hint transform is owned by its own infinite lane.
- R4a verification: site type-check, focused ESLint and diff check passed. Real Chromium at 1440x900 and 390x844 confirmed the beam variables and hint transform change over time only on the inner CineView motion nodes, all five static Scene backgrounds span the full viewport, mobile has zero horizontal overflow, and the settled Hero copy remains inside the viewport. Baseline pixel samples confirm the static Scene 1/2/3/5 gradient endpoints match the removed LUT at their captured settled positions; Scene 4 remains visually covered by its existing fullscreen media layer.
- R4b implementation: moved film pan, panel shell opacity/scale, tree nodes/wires, stagger squares, coordinate cards, and typed-code opacity onto independent scroll-owned `Animate` lanes. Timecode now reads its enclosing `useAnimateTimeline().progress`; film selection/flow and typewriter text remain unsupported DOM projections from the same MotionValue, with React state changing only when the active typed line changes. REC, perforation, and caret loops now use `infiniteAnimation`; Capability CSS has no keyframes or `animation:` declarations.
- R4b self-review: removed `panProgress`, `stageProgress`, `activeFrame`, `stripHue`, `sequenceDone`, render-prop progress probes, and all manual supported-property writers. The remaining MotionValue subscriptions clean up their listeners; film selection changes only at frame boundaries, timecode changes only at integer seconds, and no layout reads or per-frame React renders were introduced. `git diff --check`, site type-check, focused ESLint, and the old-state/keyframe/direct-motion/rAF residual scan all pass.
- R4b browser verification: fresh Chromium cold load at 1440x900 and 390x844 produced no console/page errors and zero horizontal overflow. Film pan progressed continuously to its settled position, timecode reached `00:00:10`, perforation moved while on-screen and stopped off-screen; stage panels sequenced into three columns, timecode reached `00:00:15`, all three lines completed, and reverse scroll restored the earlier panel state while reducing/clearing text. Desktop/mobile composition matches the baseline. Automated mouse movement resolved the correct frame under the pointer but did not dispatch React `onMouseEnter`, so hover-event acceptance remains explicitly unverified for the final independent pass.
- R4b reopened correction: removed the remaining film icon/caption/code/description CSS transitions and the dead panel-body/reduced-motion transition rules. Two `Animate` lanes now own selection opacity, while the caption lane also owns blur/scale; their generated keyframes preserve the existing `10500ms` scene budget and cross each discrete selection switch at zero opacity. The helper allocates its arrays once at module load, its `transition.times` are monotonic, and the MotionValue projection still mutates classes only at frame boundaries.
- R4b correction browser verification: real wheel input in Chromium sampled the second selection boundary forward and backward at 1440x900 (`1 → 0.5 → 0 → 0.5 → 1`), with the active frame/caption/code class changing only at wrapper opacity `0`. The same sequence passed at 390x844 with responsive blur `0 → 1.625px → 3.25px`, zero document/root horizontal overflow, and stable scene ownership. The perforation phase changed on-scene and remained byte-identical across two off-scene samples; fresh runtime logs contained zero warnings/errors. Browser CUA again hit the intended frame but did not dispatch React hover, so hover remains reserved for the independent acceptance pass rather than being claimed here.
- R4e owner/hot-path review: homepage Scene progress remains framework-owned. Capability selection/timecode/typewriter, Demo character color, and DragPhone reveal retain only unsupported or discrete MotionValue projections; React state changes only at typed-line or phone reveal hysteresis boundaries. Hero, REC, perforation, caret, and nested-phone loops are `infiniteAnimation` lanes. The remaining global button/language/docs-nav CSS transitions are hover/active interaction feedback, not Scene timeline writers.
- R4e snapshot verification: `scripts/capture-animation-snapshots.mjs --label animation-r4 --port 3000` produced 20/20 captures with zero console messages and zero page errors. Homepage desktop/mobile geometry matched baseline for every inspected Scene and element except the already off-screen Hero hint's framework-loop sample (`0.59px–5.43px` y drift). Homepage pixel RMSE stayed between `0.02%` and `1.29%`; side-by-side review of all ten homepage captures found no missing layer, crop change, overlap, composition shift, or typography regression. The stage caret differs only by the sampled phase of its framework-owned infinite lane.
- R4c implementation: replaced Demo subtitle's per-frame `filter`, `opacity`, and `transform` leaf writes with four line-level keyframed `Animate` lanes. A neutral `demo-subtitle` lane remains the single shared clock for the unsupported per-character `backgroundImage` color projection. The original character-level horizontal gather was deleted instead of creating about 130 `Animate` instances (and over a thousand derived MotionValues); line reveal/blur timing, final typography, framing, and per-character warm color flow remain.
- R4c self-review: the component has no render-prop progress mirror, direct motion import, React frame state, rAF, or manual supported-property writer. The only MotionValue subscription cleans up and writes `backgroundImage`; completed characters skip repeat writes until reverse scroll makes them active again. Site type-check, focused ESLint, diff check, and the supported-writer/direct-motion/keyframe residual scan pass.
- R4c browser verification: desktop Chromium forward samples showed monotonic video time and line-4 opacity/blur settling while the color endpoint advanced; reverse wheel samples reversed all three. The settled 1440x900 and 390x844 compositions match their committed scene-4 baselines, both have zero document/root horizontal overflow, and mobile blur scales with the 1440 design ruler. Browser testing caught an invalid `div`-inside-`p` warning from Animate hosts; the subtitle shell was changed to a labeled `role="group"`, and a fresh mobile cold load plus full real-wheel progression then completed with zero warning/error logs.
- R4d implementation: removed DragPhone's direct Framer Motion import, local MotionValue progress mirror, callback writer, and render-prop progress paths. The nested outgoing Scene now owns the RENDER fill/marker through exit lanes, the incoming Scene owns the ELEMENT fill through its enter lane, and REC uses phase-gated `infiniteAnimation`. The outer reveal reads its enclosing public timeline and only changes React structure at the existing 0.72/0.58 hysteresis thresholds.
- R4d self-review: the changed scope has no direct motion import, manual supported-property writer, CSS keyframe/animation, rAF, or per-frame React state. Site type-check, focused ESLint, diff check, and the full R4d bypass scan pass; unit tests remain intentionally deferred until visual sign-off as requested.
- R4d browser verification: real desktop nested drags advanced the framework-owned RENDER lane through sampled 11.4%/71.1% positions; reverse drag drove the ELEMENT lane continuously from 6.5% to 100% and committed back to Scene 1. REC opacity changed across active samples and remained byte-identical across two off-scene samples, proving the infinite lane pauses outside its phase. Mobile and desktop both completed forward/reverse commits, reveal unmounted below 0.58 and remounted above 0.72, page/root horizontal overflow stayed zero, and fresh-page logs contained no warning/error. Diagnosis also confirmed a pre-existing framework limitation: drag ratio uses `window.innerHeight` rather than the nested CineView span, so the 212.5px mobile phone requires an unusually long/fast gesture; this was reproduced against the unchanged core and deliberately not mixed into the animation-ownership remediation.
- R5 implementation: deleted the dead `useTimecode` rAF hook, moved its pure formatter to `temporal-drag/timecode.ts`, and made `phase` mandatory for Waveform, Aperture, and Clapperboard canvas contracts so their old no-phase self-start path no longer exists. Existing callers already supplied the framework phase, so the contract tightening adds no second owner or visual branch.
- R5 residual audit: the site has zero CSS `@keyframes` and zero active CSS animation declarations. Remaining CSS animation matches are reduced-motion disables; remaining CSS transitions are hover/focus/navigation feedback. The only runtime `framer-motion` import is `useReducedMotion`; three canvas imports are type-only. AmbientDust, Waveform, Aperture, and Clapperboard are the only rAF loops, all canvas-only and cancelled on `idle`/`exited`. Remaining MotionValue subscriptions project unsupported text, dataset, custom-property, background-gradient, or SVG dash output; React state changes only at typed-line and DragPhone hysteresis boundaries.
- R5 framework/build review: keyframe scrub interpolation now reads stable parsed arrays directly with no per-frame allocation, scalar variants bypass keyframe resolution, and scroll numeric/mixed properties share one pure resolver. Production entry minification enables `pure_getters` without property mangling or threshold changes. `build:verify` passed 12/12 with ES gzip 41.74 KB and UMD gzip 49.99 KB (51,190 bytes, target <= 51,200).
- R5 static verification: framework/site formatting, framework/site type-check, framework lint, site ESLint, and `git diff --check` pass. Unit tests remain deliberately deferred until user visual sign-off, so V2 is not marked complete.
- R5 browser smoke: `scripts/capture-animation-snapshots.mjs --label animation-r5 --port 3000` produced 20/20 desktop/mobile `/drag` and `/` captures with zero console messages and zero page errors. R4/R5 current-scene, element geometry, scene bounds, and horizontal-overflow metrics are identical across every capture; visual review of Acts 3-5 found no missing layer, crop, typography, overlap, or composition regression. This is implementation-side smoke only; V4 remains reserved for independent acceptance.
- V5a implementation: outgoing media now returns ownership to `framework-scrub`, invalidates its last seek, and lets the terminal `idle` frame reuse the reducer's existing progress-to-time path for one scrub-start seek. `Act3Media` clears its unsupported dataset/text/CSS-variable projection on the same `idle` handoff; `VideoFrameRenderer` remains the only `currentTime` writer.
- V5a implementation-side browser check: a fresh 1440x900 real-pointer Act 1 -> Act 2 -> Act 3 -> Act 4 -> Act 3 run reached matching `6.000s` UI/video endpoints, reset both to `0.000s` on Act 4, and re-entered Act 3 at selection 1/media 0 with both clocks still at zero. The run logged no warning or error. Independent recheck remains required before this node closes.
- V5b implementation and check: `site/index.html` now declares `lang="en"` with an English title and description. Fresh `/` and `/drag` contexts both reported the English metadata and zero visible Han characters under default storage/preferences.
- V5c diagnosis and implementation: a MutationObserver on the failing one-second reverse path showed the drag timeline publishing a terminal `progress=1` seed immediately before its real low incoming progress. Separate `progress` and `phase` subscriptions let the transport's monotonic hold latch that seed at 6s. `Act3Media` now consumes the framework's atomic `AnimateTimeline.frame`, retains its release-bounce hold, and ignores the old terminal seed after an exit until incoming progress leaves 1; it still writes no video property.
- V5c implementation-side browser check: the exact Act 3 -> Act 4, 1s wait, reverse-to-Act-3 path produced 47 visible return samples with UI/video both `0.000s` and no mismatches. The original Act 3 endpoint remained `6.000/6.000`, the returned endpoint was `0.000/0.000`, and the run logged no warnings or errors.
- V5d diagnosis and implementation: same-browser isolation at 1440x900 showed baseline p95 `33.4ms`, canvas-hidden p95 `33.4ms`, and light-rig-hidden p95 `17.1ms`. Per-layer combinations identified the continuously transformed full-frame masked beam over the other gradients as the binding composition cost. SlateLightRig now retains every layer's framework-owned enter/exit and full static settled composition but has no continuous full-frame transform; Act 2's phase-gated particle canvas and framework-owned board micro-motion remain active.
- V5d verification: fresh source-state Act 2 measured p50 `16.7ms`, p95 `17.2ms`, max `18.7ms`, zero intervals over `34ms`, and zero Long Tasks across four seconds. Desktop/mobile screenshots preserve composition and crop against R5 with `1.276%`/`1.831%` RMSE; the difference is the intentionally removed settled light-loop phase.
- V5 final static verification: root/site type checks, full root lint, site formatting, `git diff --check`, and the site production build pass. The Act 2 static contract now pins the absence of a full-frame light `infiniteAnimation`. Jest remains intentionally deferred until visual sign-off, so V2 stays open.
- V4/V5 independent acceptance: a separate headed Chromium lane confirmed English metadata and zero default visible Han text; the exact Act 3 -> Act 4 -> 1s -> reverse path held dataset/UI/video at `0.000/00:00:00:00/0` for every visible, release, and settle sample; desktop Act 1 and settled Act 2 both measured p50 `16.7ms`, p95 `18.6ms`, max `18.7ms`, zero intervals over `34ms`, and zero Long Tasks over four seconds.
- V4/V5 independent regression: desktop and 390x844 mobile completed real-pointer `/drag` `1 -> 5 -> 1` with every landing at `top=0`; Act 4 closed to `0.800` and reopened continuously to `0` with timecode reset before its first fade sample; Act 5 retained moving beam/dust, five English credits and THE END with no screen/wall/seat; mobile had zero horizontal overflow. Homepage real-wheel smoke advanced normally. Console warnings/errors were `0/0`.
