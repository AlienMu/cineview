# Cloudflare Pages provides SPA fallback when no top-level 404.html exists.
# React Router handles /docs, /docs/*, and /drag without redirect rules.
# https://developers.cloudflare.com/pages/configuration/serving-pages/
