/**
 * Kept as a source-compatible page entry for local links from older builds.
 * CineView now exposes only drag and scroll modes; the old snap mode is the
 * drag scene stack with an animated settle.
 */
export { default } from './DragModePage';
