# 用户决策执行计划

## 背景

用户明确三项决策（2026-09-03）：

1. **依赖升级** - 执行完整的大版本升级
2. **仓库体积** - 删除历史任务流和验收文件
3. **语言策略** - 内核改为全英文，docs/README 保持双语

## 执行中工作流 (wf_4c9ae412-a5c)

### Phase 1: 依赖升级（9 个包，顺序执行）

| 包                        | 版本    | 风险     | 说明                            |
| ------------------------- | ------- | -------- | ------------------------------- |
| @testing-library/jest-dom | ^6→^7   | 低       | 测试工具                        |
| @fast-check/jest          | ^1→^2   | 低       | 属性测试                        |
| @types/node               | ^25→^26 | 低       | Node 类型                       |
| **typescript**            | ^5→^7   | **高**   | 编译器大版本，有 5 处预知修复点 |
| @types/react              | ^18→^19 | 中       | React 类型                      |
| @types/react-dom          | ^18→^19 | 中       | ReactDOM 类型                   |
| **react**                 | ^18→^19 | **高**   | dev 依赖，影响测试              |
| **react-dom**             | ^18→^19 | **高**   | dev 依赖，影响测试              |
| **framer-motion**         | ^11→^13 | **危险** | 核心动画引擎跨 2 大版本         |

每个升级独立验证：type-check + test:unit，失败则修复后继续。

### Phase 2: 删除历史文件

删除目标：

- `task-flows/` - 所有执行日志（本轮刚写的也删，因为是验收记录）
- `site/review/` - 38MB 设计验收截图/录屏
- 其他验收工件

保留：

- `src/` 全部源码
- `docs/` 全部文档
- `DESIGN.md` (Phase 3 翻译)
- 配置文件、构建脚本
- README 双语文件

### Phase 3: 翻译为英文

翻译范围：

- `DESIGN.md` - 2763 行，约 1334 行中文 → 全英文
- `src/` 注释 - 100 个文件中 42 个含中文 → 全英文注释

保持双语：

- `docs/src/content/docs/{en,zh}/` - 已经是双语结构
- `README.md` / `README.zh-CN.md` - 保持双语

验收：

- DESIGN.md 零中文字符
- src/ 零中文注释（字面量除外）
- 全部测试通过

## 预期成本

- 11 个并行 agents（9 升级 + 1 清理 + 1 翻译）
- 约 60-90 分钟墙钟时间
- 高风险：framer-motion 跨 2 大版本可能有未预见的破坏性变更

## 回退策略

- Git 历史完整，当前在 commit 642a211
- 可以 `git revert` 任何失败的步骤
- 如果 framer-motion 升级失败，可以单独回退该包
